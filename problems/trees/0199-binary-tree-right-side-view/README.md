# 199. Binary Tree Right Side View

- URL: https://leetcode.com/problems/binary-tree-right-side-view/
- 難易度: medium
- カテゴリ: trees
- 初回: 2026-09-02

## 復習ログ

<!-- ATTEMPTS:START -->

| 回 | 日付 | 結果 | 所要時間 | コード | メモ |
| --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-02 | AC | - | [solution.py](solution.py) | Beats 100%。102 の「層ごとの区切り」をそのまま適用 |

<!-- ATTEMPTS:END -->

## 問題の要約

木を右から見たときに見える値を、上の層から順に返す。
= **各層でいちばん右にあるノードの値**を集める。

## 考えたこと / アプローチ

<!-- ここは本人が手で書く欄。Claude は見出しだけ用意し、中身は書かない。 -->

### 1 回目

<!-- 最初に思いついた方針、詰まった点、気づき -->

## 計算量

- 時間: O(n) — 各ノードを 1 回ずつ訪れる
- 空間: O(w) — queue。w = 最大幅。最悪 O(n)

## 学び・再利用できるパターン

- **[102. Level Order](../0102-binary-tree-level-order-traversal) の型がそのまま効く**。
  `len(q)` で層を区切り、その回数だけ取り出す。違いは
  「層の値を全部集める」か「最後の 1 つだけ残す」かだけ
- 層の値を溜めるリストすら要らない。ループ変数 `node` は最後に取り出したノードを
  指したまま残るので、ループを抜けた直後に `node.val` を見れば
  **その層のいちばん右**が取れる
- 「右から見える」= 「各層の最右」という**言い換えが本質**。
  これに気づけば、木を右から眺める必要はなく、いつもの層ごとの BFS になる
- 右の部分木が浅いときは、下の層で左側のノードが「見える」。
  `root.right` だけを辿る解法では間違える（テストの
  `test_left_subtree_is_deeper` がそのケース）

### 102 との関係（時系列）

- **8/30（[102](../0102-binary-tree-level-order-traversal)）**: `len(queue)` で層を区切る形を自力で発見。
  1 回目は `list.pop(0)`、2 回目で `deque` に改善
- **9/2（この問題）**: 最初から `deque` + `len(q)` で書けている。Beats 100%

102 で作った型を、別の問題で迷わず再利用できている。
